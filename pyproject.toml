[project]
name = "Search Engine"
version = "0.1.0"
description = "Search Engine for a specific domain (AI, Coding, Research Paper)"
readme = "reame.md"
requires-python = ">=3.14"
dependencies = [
    "python >= 3.14.5",
    "crawl4ai >= 0.9.0",
]